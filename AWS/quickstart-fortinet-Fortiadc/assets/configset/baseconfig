
config system auto-scale
    set status enable
    set sync-interface "{SYNC_INTERFACE}"
    set role primary
    set callback-url {CALLBACK_URL}
end

config system global
    set port-https {ADMIN_PORT}
    set port-http 8080
	set cloud-autoscale enable
end
